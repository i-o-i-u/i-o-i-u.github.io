+++
title = "بيتٌ للتخرُّج"
slug = "takharog"
date = 2024-05-19
+++

<div class="glass-premium-box">
  <p class="text-xl md:text-2xl leading-loose text-gray-800 dark-white-text">
    السؤال: 

اقترح لي بيتًا يصلح أن يوضع على دفتر التخرج.
  </p>
</div>

<style>
/* ===== التصميم النهاري (أصلي كما في الصندوق الثاني) ===== */
.glass-premium-box {
  margin: 2.5rem 0;            /* my-10 */
  padding: 2rem;               /* p-8 */
  border-radius: 1rem;         /* rounded-2xl ≈ 1rem */
  background: linear-gradient(135deg, rgba(255, 237, 213, 0.5), rgba(253, 230, 138, 0.5)); /* from-orange-100/50 to-amber-100/50 */
  border: 1px solid #fed7aa;   /* border-orange-200 */
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05); /* shadow-lg */
  color: #1f2937;              /* text-gray-800 */
}

/* ===== الوضع الليلي: تأثير زجاجي أسود-رمادي فخم ===== */
body.dark .glass-premium-box {
  background: rgba(18, 18, 22, 0.7);   /* خلفية سوداء مائلة للرمادي مع شفافية */
  backdrop-filter: blur(12px);         /* تأثير الزجاج */
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.12);  /* حد أبيض شفاف ناعم */
  box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.4); /* ظل عميق */
  color: #f3f4f6;                      /* نص أبيض ناعم */
}

/* تأمين لون النص في الوضع الليلي */
body.dark .dark-white-text {
  color: #f3f4f6 !important;
}
</style>

---

**الجواب:**

والله لا أستحضر شيئًا مُعيَّنًا في هذا المعنى، لكنَّ ذلك على حسب المعنى الذي تريده أنت، فإن كانت حالتك أنك كابدتَّ الجهد والمشقَّة في دراستك حتى تخرَّجت، فلك أن تتمثَّل بقول حاجر بن عوف الأزدي: 

<div class="glass-premium-box">
  <table class="poetry-table">
    <tr>
      <td class="shatr first">صَلِيتُ بغَمْرةٍ فخرجتُ منها</td>
      <td class="shatr last">كنصْل السيف مُختضِبَ الغِرار!</td>
    </tr>
  </table>
</div>

أو كانت حالُك أنك كنتَ ماضيَ العزْم، نافذَ الهمِّ، ألوى المريرة، أخَّاذًا بالحزْم والجِدِّ في دراستك، تمثَّلت بقول ابن هانئ:

<div class="glass-premium-box">
  <table class="poetry-table">
    <tr>
      <td class="shatr first">ولم أجدِ الإنسانَ إلا ابنَ سعْيِه</td>
      <td class="shatr last">فمن كانَ أسعى كان بالمجدِ أجدرَا</td>
    </tr>
    <tr>
      <td class="shatr first">وبالهمَّة العَلْياء يُرقى إلى العُلا</td>
      <td class="shatr last">فمن كان أرقى همةً كان أظهرا</td>
    </tr>
    <tr>
      <td class="shatr first">ولم يتأخَّرْ من يريدُ تقدُّمًا</td>
      <td class="shatr last">ولم يتقدَّمْ مَن يريدُ تأخُّرَا</td>
    </tr>
  </table>
</div>

وإذا كان المعنى الذي تريده إظهار الغبطة بنَجاز ما تعبتَ من أجله قلتَ: 

<div class="glass-premium-box">
  <table class="poetry-table">
    <tr>
      <td class="shatr first">من بعد أيَّام المشقَّة والعَنا</td>
      <td class="shatr last">قرَّ الفؤادُ وقرَّتِ العينانِ</td>
    </tr>
  </table>
</div>

ويجوز ضَمُّ نون (العينان)، على لغةٍ لبعض العرب، وعليه ما أنشده الآمديُّ في المؤتلف والمختلف لأبي بيهس رؤبة بن العجَّاج بن شدقم الباهلي:

<div class="glass-rajaz-box">
  <table class="rajaz-table">
    <tr><td class="rajaz-shatr">فالنومُ لا تطعَمُه العَيْنانُ</td></tr>
    <tr><td class="rajaz-shatr">من وخْز بُرْغوثٍ له أسنانُ</td></tr>
  </table>
</div>


وإن كان مقصودك حمدَ الله على ما أولى به فقل:

<div class="glass-premium-box">
  <table class="poetry-table">
    <tr>
      <td class="shatr first">الحمد لله حمدًا لا انقطاعَ له</td>
      <td class="shatr last">فليس إحسانُه عنَّا بمقطوعِ</td>
    </tr>
  </table>
</div>


أو كانت حالك أنك بلغتَ أعلى المراتب، وحُزتَ أسنى الدرجات، تمثَّلتَ بقول القائل:

<div class="glass-rajaz-box">
  <table class="rajaz-table">
    <tr><td class="rajaz-shatr">غايةُ مجْدٍ رُفعت، فمَن لها؟</td></tr>
    <tr><td class="rajaz-shatr">نحن حَويناها وكنَّا أهلَها</td></tr>
    <tr><td class="rajaz-shatr">لو أُرسِل الرِّيحُ لجئنا قبلَها!</td></tr>
  </table>
</div>

أو كان شأنك أن أمورك كانت تمشي بالتوفيق والبرَكة، من غير عَناءٍ منك وكبَد، قلتَ:

<div class="glass-premium-box">
  <table class="poetry-table">
    <tr>
      <td class="shatr first">فللّٰهِ دَرِّي يومَ أدخلُ ساحةً</td>
      <td class="shatr last">تلوحُ بها الأهوالُ، والموتُ يَخْفِقُ</td>
    </tr>
    <tr>
      <td class="shatr first">وأدخُل لا درعًا لبستُ، ولا لها</td>
      <td class="shatr last">حملتُ سلاحًا، غيرَ أني مُوَفَّقُ :)</td>
    </tr>
  </table>
</div>

وهكذا، والله أعلم.

<style>
/* ===== القصيدة العمودية ===== */
.glass-premium-box {
  margin: 2.5rem auto;
  padding: 1.5rem 2rem;
  border-radius: 1rem;
  max-width: 680px;
  width: 100%;
  box-sizing: border-box;
  background: linear-gradient(135deg, rgba(255,237,213,0.5), rgba(253,230,138,0.5));
  border: 1px solid #fed7aa;
  box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
  color: #1f2937;
}
body.dark .glass-premium-box {
  background: rgba(18,18,22,0.7);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255,255,255,0.12);
  box-shadow: 0 20px 40px -10px rgba(0,0,0,0.4);
  color: #f3f4f6;
}
.poetry-table {
  width: 100%;
  border-collapse: collapse;
  direction: rtl;
  font-family: 'Amiri', 'Traditional Arabic', 'Scheherazade', serif;
  font-size: 1.25rem;
  table-layout: fixed;
}
.shatr {
  width: 50%;
  padding: 0.4rem 0.6rem;
  line-height: 2.2;
  vertical-align: baseline;
  white-space: nowrap;
  overflow: hidden;
}
.shatr.first {
  border-left: 2px solid rgba(180,130,50,0.4);
  text-align: right;
}
.shatr.last {
  text-align: right;
}
body.dark .shatr.first {
  border-left-color: rgba(255,255,255,0.15);
}

/* ===== الرجز ===== */
.glass-rajaz-box {
  margin: 2.5rem auto;
  padding: 1.5rem 2rem;
  border-radius: 1rem;
  max-width: 400px;
  width: 100%;
  box-sizing: border-box;
  background: linear-gradient(135deg, rgba(255,237,213,0.5), rgba(253,230,138,0.5));
  border: 1px solid #fed7aa;
  box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
  color: #1f2937;
}
body.dark .glass-rajaz-box {
  background: rgba(18,18,22,0.7);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255,255,255,0.12);
  box-shadow: 0 20px 40px -10px rgba(0,0,0,0.4);
  color: #f3f4f6;
}
.rajaz-table {
  width: 100%;
  border-collapse: collapse;
  direction: rtl;
  font-family: 'Amiri', 'Traditional Arabic', 'Scheherazade', serif;
  font-size: 1.25rem;
  table-layout: fixed;
}
.rajaz-shatr {
  width: 100%;
  padding: 0.3rem 0.6rem;
  line-height: 2.2;
  vertical-align: baseline;
  white-space: nowrap;
  overflow: hidden;
  text-align: right;
}

/* ===== الجوال ===== */
@media (max-width: 640px) {
  .glass-premium-box, .glass-rajaz-box {
    padding: 0.75rem;
    margin: 1rem auto;
  }
  .poetry-table, .rajaz-table {
    font-size: 0.78rem;
  }
  .shatr {
    white-space: nowrap;
    overflow: hidden;
    padding: 0.25rem 0.3rem;
    line-height: 2;
  }
  .shatr.first {
    border-left: 1px solid rgba(180,130,50,0.3);
  }
  .rajaz-shatr {
    padding: 0.25rem 0.3rem;
    line-height: 2;
  }
}
</style>

<script>
const TASHKEEL = /[\u064B-\u065F\u0670]/;
const NO_CONNECT_AFTER = new Set(['ا','أ','إ','آ','ٱ','و','ؤ','ز','ذ','د','ر','ى','ة']);
const PUNCT = new Set(['،','؛','؟','!','.',',',':','-','–','—','«','»','"','"','(',')','/']);
const FORBIDDEN_WORDS = new Set(['الله','اللهم','بالله','تالله','والله','فالله','لله']);

function tokenize(word) {
  const tokens = [];
  for (let i = 0; i < word.length; i++) {
    if (TASHKEEL.test(word[i])) {
      if (tokens.length) tokens[tokens.length - 1].marks += word[i];
    } else {
      tokens.push({ base: word[i], marks: '' });
    }
  }
  return tokens;
}

function tokensToString(tokens) {
  return tokens.map(t => t.base + t.marks).join('');
}

function canConnectAfter(base) {
  return !NO_CONNECT_AFTER.has(base) && !PUNCT.has(base) && /[\u0600-\u06FF]/.test(base);
}

function getBestInsertPos(tokens) {
  let lastValid = tokens.length - 1;
  while (lastValid > 0 && PUNCT.has(tokens[lastValid].base)) lastValid--;
  for (let i = lastValid - 1; i >= 1; i--) {
    if (PUNCT.has(tokens[i + 1]?.base)) continue;
    const nextBase = tokens[i + 1]?.base;
    if (tokens[i].base === 'ل' && nextBase && 'اأإآٱ'.includes(nextBase)) continue;
    if (canConnectAfter(tokens[i].base)) return i + 1;
  }
  return -1;
}

function measureText(text, font) {
  const c = measureText._canvas || (measureText._canvas = document.createElement('canvas'));
  const ctx = c.getContext('2d');
  ctx.font = font;
  return ctx.measureText(text).width;
}

function stretchCell(cell, font) {
  const available = cell.clientWidth
    - parseFloat(getComputedStyle(cell).paddingLeft)
    - parseFloat(getComputedStyle(cell).paddingRight);
  if (!cell.dataset.original) cell.dataset.original = cell.textContent.trim();
  const originalWords = cell.dataset.original.split(/\s+/);
  const words = originalWords.map(w => ({ tokens: tokenize(w), tatweels: 0 }));

  for (let round = 0; round < 80; round++) {
    // ← الإصلاح: join بمسافة
    const currentText = words.map(w => tokensToString(w.tokens)).join(' ');
    const gap = available - measureText(currentText, font);
    if (gap <= 0.5) break;
    const candidates = words
      .map((w, i) => ({
        i,
        pos: getBestInsertPos(w.tokens),
        tatweels: w.tatweels,
        len: w.tokens.length,
        orig: originalWords[i].replace(/[\u064B-\u065F\u0670]/g, '')
      }))
      .filter(c => c.pos !== -1 && ![...FORBIDDEN_WORDS].some(fw => c.orig.includes(fw)))
      .sort((a, b) => a.tatweels - b.tatweels || b.len - a.len);
    if (!candidates.length) break;
    const best = candidates[0];
    words[best.i].tokens.splice(best.pos, 0, { base: 'ـ', marks: '' });
    words[best.i].tatweels++;
  }
  // ← الإصلاح: join بمسافة
  cell.textContent = words.map(w => tokensToString(w.tokens)).join(' ');
}

function stretchAll() {
  document.querySelectorAll('.poetry-table').forEach(table => {
    table.querySelectorAll('.shatr').forEach(cell => {
      if (cell.dataset.original) cell.textContent = cell.dataset.original;
    });
    const sample = table.querySelector('.shatr');
    if (!sample) return;
    const cs = getComputedStyle(sample);
    const font = `${cs.fontStyle} ${cs.fontWeight} ${cs.fontSize}/${cs.lineHeight} ${cs.fontFamily}`;
    table.querySelectorAll('.shatr').forEach(cell => stretchCell(cell, font));
  });
  document.querySelectorAll('.rajaz-table').forEach(table => {
    table.querySelectorAll('.rajaz-shatr').forEach(cell => {
      if (cell.dataset.original) cell.textContent = cell.dataset.original;
    });
    const sample = table.querySelector('.rajaz-shatr');
    if (!sample) return;
    const cs = getComputedStyle(sample);
    const font = `${cs.fontStyle} ${cs.fontWeight} ${cs.fontSize}/${cs.lineHeight} ${cs.fontFamily}`;
    table.querySelectorAll('.rajaz-shatr').forEach(cell => stretchCell(cell, font));
  });
}

document.addEventListener('copy', function(e) {
  const selection = window.getSelection();
  if (!selection || selection.isCollapsed) return;
  const text = selection.toString().replace(/ـ/g, '');
  e.clipboardData.setData('text/plain', text);
  e.preventDefault();
}, true); // ← true هنا هو المهم، يجعله يعمل قبل أي معترض آخر

if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', stretchAll);
} else {
  stretchAll();
}
window.addEventListener('resize', stretchAll);
</script>